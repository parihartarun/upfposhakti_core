version: "3.5"

services:
    fpoapp:
      build:
        context: ./
        dockerfile: Dockerfile
      container_name: core
      ports:
        - 8082:8085
        - 80:8085
      volumes:
        - .:/code
