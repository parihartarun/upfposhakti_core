# UP FPO
 
![Java CI with Maven](https://github.com/nishantshah-me/upfpo/workflows/Java%20CI%20with%20Maven/badge.svg)


### Git basic commands

####  Create a branch

`git checkout -b {your_branch_name}`

#### Push into a branch

`git add --all`

`git commit -m "message"`

`git push origin {your_branch_name}`

#### pull from branch

`git pull origin develop`